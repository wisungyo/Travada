<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.travada">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".features.tabungan.detail_tabungan.DetailTabunganActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".features.tabungan.form_result_detail.DetailFormResultActivity">

        </activity>
        <activity android:name=".features.tabungan.form_three.FormTabunganThreeActivity" />
        <activity android:name=".features.tabungan.form_two.FormTabunganTwoActivity" />
        <activity android:name=".features.tabungan.form_one.FormTabunganOneActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".features.tabungan.MainTabungan.TabunganActivity">

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>